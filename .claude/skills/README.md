# Skills

Drop skill definition files (`.md`) here. Each file defines a `/command` that Claude Code can invoke.

To register a skill, add it to `AGENTS.md` or `CLAUDE.md` with:
```
/skill-name: <description>
```
